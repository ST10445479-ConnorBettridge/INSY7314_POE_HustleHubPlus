# The Gradle wrapper is a shell script: CRLF breaks the shebang on Unix.
gradlew text eol=lf
*.sh text eol=lf

# Windows batch files need CRLF.
*.bat text eol=crlf

# Never touch binaries.
*.jar binary
*.png binary
*.docx binary
*.pem -text
